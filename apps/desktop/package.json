{
  "name": "@z-assistant/app-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "V2 Desktop App — placeholder for future phase. Per ADR §二, Phase 6A is NOT allowed to implement it (Electron / Tauri / Native).",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p ./",
    "typecheck": "tsc -p ./ --noEmit",
    "clean": "rm -rf out dist *.tsbuildinfo"
  },
  "dependencies": {
    "@z-assistant/contracts": "0.1.0",
    "@z-assistant/runtime": "0.1.0"
  }
}
