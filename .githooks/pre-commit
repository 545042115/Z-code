#!/bin/sh
# Pre-commit hook: run TypeScript typecheck before allowing a commit.
# Install with: git config core.hooksPath .githooks

echo "Running npm run typecheck..."
npm run typecheck
if [ $? -ne 0 ]; then
  echo "Typecheck failed. Commit aborted."
  exit 1
fi
