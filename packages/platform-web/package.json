{
  "name": "@ziner/platform-web",
  "version": "0.1.0",
  "private": true,
  "description": "Web/Mobile platform implementation for runtime-core interfaces. Uses IndexedDB and Capacitor plugins.",
  "main": "out/index.js",
  "types": "out/index.d.ts",
  "scripts": {
    "build": "tsc -p ./",
    "typecheck": "tsc -p ./ --noEmit",
    "clean": "rm -rf out *.tsbuildinfo"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "*",
    "@ziner/contracts": "*",
    "@ziner/runtime-core": "*"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
