# @z-assistant/contracts

Cross-package type contracts for the V2 Assistant Runtime. **Pure types only**.

- No runtime side effects.
- No `vscode` imports.
- No `fs` / `http` / DOM dependencies.

Phase 6A: placeholder. Real interfaces are added in R3+ as V1 code is migrated.
