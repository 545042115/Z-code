{
  "name": "@ziner/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "V2 cross-package type contracts. Pure types only — no runtime dependencies, no vscode imports.",
  "main": "out/index.js",
  "types": "out/index.d.ts",
  "scripts": {
    "build": "tsc -p ./",
    "typecheck": "tsc -p ./ --noEmit",
    "clean": "rm -rf out dist *.tsbuildinfo"
  }
}
