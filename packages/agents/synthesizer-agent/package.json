{
  "name": "@ziner/agent-synthesizer",
  "version": "0.1.0",
  "private": true,
  "description": "V2 Synthesizer Agent — aggregates multi-agent / sub-task outputs into a coherent user-facing answer (P2 multi-agent).",
  "main": "out/index.js",
  "types": "out/index.d.ts",
  "scripts": {
    "build": "tsc -p ./",
    "typecheck": "tsc -p ./ --noEmit",
    "clean": "rm -rf out dist *.tsbuildinfo"
  },
  "dependencies": {
    "@ziner/contracts": "0.1.0"
  }
}
