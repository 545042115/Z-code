{
  "name": "@ziner/agent-office",
  "version": "0.1.0",
  "private": true,
  "description": "V2 Office Agent — placeholder for future phase. Per ADR §二, Phase 6A is NOT allowed to implement it.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p ./",
    "typecheck": "tsc -p ./ --noEmit",
    "clean": "rm -rf out dist *.tsbuildinfo"
  },
  "dependencies": {
    "@ziner/contracts": "0.1.0",
    "docx": "^9.0.0",
    "xlsx": "^0.18.5",
    "pptxgenjs": "^3.12.0"
  }
}
