{
  "name": "@ziner/agent-research",
  "version": "0.1.0",
  "private": true,
  "description": "V2 Research Agent. Performs deep research via search, fetch, and synthesis loops.",
  "main": "out/index.js",
  "types": "out/index.d.ts",
  "scripts": {
    "build": "tsc -p ./",
    "typecheck": "tsc -p ./ --noEmit",
    "test": "tsc -p ./ && node --test \"out/__tests__/*.test.js\"",
    "clean": "rm -rf out dist *.tsbuildinfo"
  },
  "dependencies": {
    "@ziner/contracts": "0.1.0",
    "@ziner/runtime": "0.1.0",
    "@ziner/trace": "0.1.0"
  }
}
