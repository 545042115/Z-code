{
  "name": "@z-assistant/agent-research",
  "version": "0.1.0",
  "private": true,
  "description": "V2 Research Agent — placeholder for future phase. Per ADR §二, Phase 6A is NOT allowed to implement it.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p ./",
    "typecheck": "tsc -p ./ --noEmit",
    "clean": "rm -rf out dist *.tsbuildinfo"
  },
  "dependencies": {
    "@z-assistant/contracts": "0.1.0"
  }
}
