# @ziner/runtime — Python Perception Server dependencies
# Install: pip install -r python/requirements.txt
#
# All models use ONNX Runtime — no PyTorch needed.
# Total download: ~150MB (models downloaded on first use)

# ── Core ──────────────────────────────────────────
Pillow>=10.0.0

# ── ONNX Runtime (CPU, ~10MB) ─────────────────────
onnxruntime>=1.17.0

# ── OCR: Surya (ONNX-based, ~50MB model) ──────────
surya-ocr>=0.4.0

# ── Audio Transcription: faster-whisper (CTranslate2, ~80MB model) ──
faster-whisper>=1.0.0

# ── Document Parsing (pure Python/C, no ML) ───────
PyMuPDF>=1.23.0
python-docx>=1.1.0
python-pptx>=0.6.23

# ── PyInstaller (for packaging, optional) ─────────
# pyinstaller>=6.0.0
