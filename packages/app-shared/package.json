{
  "name": "@ziner/app-shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared application logic between Desktop and Mobile. UI-agnostic business logic.",
  "main": "out/index.js",
  "types": "out/index.d.ts",
  "scripts": {
    "build": "tsc -p ./",
    "typecheck": "tsc -p ./ --noEmit",
    "clean": "rm -rf out *.tsbuildinfo"
  },
  "dependencies": {
    "@ziner/contracts": "*",
    "@ziner/runtime-core": "*"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
